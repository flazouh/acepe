Organize one folder per concern: errors, constants, types, components, logic, hooks, and tests; Follow the orchestrator–hook–UI pattern.

We will use neverthrow and return Result for all.
We will define errors.
We will have bun tests for all the logic, with 100% coverage.
We will create one file per type, component, and others.
We will write JS docs.
We will use neverthrow, no promises, and no voids.
