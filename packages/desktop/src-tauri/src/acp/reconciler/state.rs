//! Per-session / per-tool-call reducer state (Unit 3–4).
//!
//! Placeholder so orchestration can own cleanup at terminal events without growing `mod.rs`.

#[derive(Debug, Default)]
pub struct ProviderReducerState {}
