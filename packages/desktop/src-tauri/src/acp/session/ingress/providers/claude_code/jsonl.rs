//! Claude Code NDJSON ingress helpers — Phase 3 home for `acp::session::ingress::jsonl`.

pub(crate) use crate::acp::session::ingress::jsonl::{
    parse_jsonl_file, session_updates_to_provider_events,
};
