/**
 * Constants for the agent input component.
 *
 * Centralized definitions prevent magic numbers and strings,
 * ensuring consistency across the component.
 */

/**
 * Character that triggers the file picker dropdown.
 */
export const FILE_PICKER_TRIGGER = "@" as const;

/**
 * Character that triggers the slash command dropdown.
 */
export const SLASH_COMMAND_TRIGGER = "/" as const;
