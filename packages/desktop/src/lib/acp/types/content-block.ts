/**
 * Re-export content block types from generated Rust types (via specta).
 *
 * These types are now defined in Rust and generated via specta.
 */
export type { ContentBlock, EmbeddedResource } from "../../services/converted-session-types.js";
