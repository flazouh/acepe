/**
 * Default empty state message when no items are available.
 */
export const DEFAULT_EMPTY_MESSAGE = "No items available";

/**
 * Default empty state message for groups.
 */
export const DEFAULT_GROUP_EMPTY_MESSAGE = "No items in this group";
