/**
 * Spacing between the composer's trailing controls.
 *
 * The model pill, the mic and the submit button sit in two sibling flex
 * containers, so both have to declare the same gap for the row to read as one
 * evenly spaced strip.
 */
export const AGENT_INPUT_CONTROL_GAP_CLASS = "gap-1";
