/**
 * Shared header row for tool cards and user-message shells.
 *
 * `h-6` + trailing `icon-sm` (`size-5`) leaves 2px vertical inset; `pr-0.5`
 * matches that on the right so corner controls sit square in the card.
 */
export const TOOL_CARD_HEADER_ROW_CLASS =
	"flex h-6 items-center justify-between gap-1.5 pl-2 pr-0.5 text-sm";
