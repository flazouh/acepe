---
title: "AC-033: projection.pending-approvals"
type: rebuild
status: todo
wave: "Wave 5 — Projections fan-out"
size: M
blocked_by: [AC-012, AC-022]
date: 2026-08-20
---

# AC-033 — projection.pending-approvals

**Wave:** Wave 5 — Projections fan-out  ·  **Size:** M  ·  **Blocked by:** AC-012, AC-022
**Blocks:** AC-046

## Goal

Materialise outstanding permission requests so the UI can prompt without polling the provider.

## Files

- `packages/server/src/persistence/Services/ProjectionPendingApprovals.ts`
- `packages/server/src/persistence/Layers/ProjectionPendingApprovals.ts`

## Acceptance criteria

1. Table `projection_pending_approvals`, rebuildable from the event log alone.
2. An approval outlives a process restart and is still answerable afterwards.
3. Answering an approval twice is idempotent via the command receipt.
4. `rebuild('projection.pending-approvals')` reproduces the table identically.
5. Graded against the reference fixture through `ProjectionSnapshotQuery`.

## Notes

Fully parallel with the other Wave 5 tickets: no shared files, no shared tables.

## Definition of done

- All acceptance criteria above pass.
- Colocated `.test.ts` exists beside every new source file.
- `bun run typecheck` and the full test suite pass.
- `god-architecture-check` was run if this ticket touches canonical session state.
