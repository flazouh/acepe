---
title: "AC-046: Parity burn-down"
type: rebuild
status: todo
wave: "Wave 8 — Cutover"
size: L
blocked_by: [AC-024, AC-025, AC-026, AC-027, AC-028, AC-030, AC-031, AC-032, AC-033, AC-034, AC-035, AC-036, AC-037, AC-038, AC-039, AC-040, AC-041, AC-042, AC-043]
date: 2026-08-20
---

# AC-046 — Parity burn-down

**Wave:** Wave 8 — Cutover  ·  **Size:** L  ·  **Blocked by:** AC-024, AC-025, AC-026, AC-027, AC-028, AC-030, AC-031, AC-032, AC-033, AC-034, AC-035, AC-036, AC-037, AC-038, AC-039, AC-040, AC-041, AC-042, AC-043
**Blocks:** AC-047

## Goal

Drive every recorded fixture to green and close the gap list before the sidecar is removed.

## Files

- `packages/harness/`

## Acceptance criteria

1. Every committed fixture passes the harness against the TypeScript implementation.
2. Accepted losses, mainly ACP fidelity gaps, are listed in one document with a decision recorded for each.
3. You have used the Electrobun build as your daily driver for two full weeks with the sidecar disabled.
4. `thermo-nuclear-code-quality-review` has been run over the whole rebuild and its findings are either fixed or waived in writing.

## Notes

Do not skip the two-week daily-driver criterion. Fixtures catch behaviour you recorded; daily use catches behaviour you never thought to record.

## Inherited parity gaps

These lanes shipped with their own fixtures instead of grading against the recorded sidecar traffic, so parity with the Rust implementation is unproven for each. Grading them is part of this ticket.

- **AC-040** history importers: criterion 2 asked for byte-identical projections graded against the Rust importer.
- **AC-024** Claude adapter: criterion 4 asked for grading against the reference fixture at 100 percent.

The cause was a dispatch prompt that emphasised typecheck and lint but never named the harness. Fixed in `scripts/dispatch-ticket.sh`, but these two predate the fix.

## Missing recorded fixtures

Grading is blocked for every provider except Claude because no recorded traffic exists. `packages/harness/fixtures` holds only `claude-session-reference.ndjson` (5 lines) and `tracer-bullet-reference.ndjson`.

Before this ticket can burn down, record real sessions with `bun run harness` for: Codex, Cursor, Copilot, OpenCode. Record from daily use, not synthetic scripts; the grader is only as good as what was captured.

AC-027 correctly refused to write a substitute fixture and reported its criterion blocked. That is the expected behaviour for every lane in this situation.

## Definition of done

- All acceptance criteria above pass.
- Colocated `.test.ts` exists beside every new source file.
- `bun run typecheck` and the full test suite pass.
- `god-architecture-check` was run if this ticket touches canonical session state.
