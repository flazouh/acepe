---
title: "AC-031: projection.session-activities"
type: rebuild
status: todo
wave: "Wave 5 — Projections fan-out"
size: M
blocked_by: [AC-012, AC-022]
date: 2026-08-20
---

# AC-031 — projection.session-activities

**Wave:** Wave 5 — Projections fan-out  ·  **Size:** M  ·  **Blocked by:** AC-012, AC-022
**Blocks:** AC-046

## Goal

Materialise the activity feed: tool calls, file operations, and their status transitions.

## Files

- `packages/server/src/persistence/Services/ProjectionSessionActivities.ts`
- `packages/server/src/persistence/Layers/ProjectionSessionActivities.ts`

## Acceptance criteria

1. Table `projection_session_activities`, rebuildable from the event log alone.
2. Tool rows link to their operation when one exists, and render correctly while the link is still pending.
3. Status transitions are ordered by event sequence and never regress.
4. `rebuild('projection.session-activities')` reproduces the table identically.
5. Graded against the reference fixture through `ProjectionSnapshotQuery`.

## Notes

Fully parallel with the other Wave 5 tickets: no shared files, no shared tables.

## Definition of done

- All acceptance criteria above pass.
- Colocated `.test.ts` exists beside every new source file.
- `bun run typecheck` and the full test suite pass.
- `god-architecture-check` was run if this ticket touches canonical session state.
