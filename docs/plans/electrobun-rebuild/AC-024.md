---
title: "AC-024: Claude adapter via claude-agent-sdk"
type: rebuild
status: todo
wave: "Wave 4 — Providers"
size: L
blocked_by: [AC-023]
date: 2026-08-20
---

# AC-024 — Claude adapter via claude-agent-sdk

**Wave:** Wave 4 — Providers  ·  **Size:** L  ·  **Blocked by:** AC-023
**Blocks:** AC-046

## Goal

Port Acepe's richest provider path, keeping the SDK-direct fidelity rather than dropping to ACP.

## Files

- `packages/server/src/provider/Layers/ClaudeAdapter.ts`
- `packages/server/src/provider/Layers/ClaudeProvider.ts`

## Acceptance criteria

1. Uses `@anthropic-ai/claude-agent-sdk` directly, matching both t3code's choice and Acepe's existing `cc_sdk` path.
2. Streaming deltas, tool calls, permission requests, plan proposals, compaction and usage all map to contract events.
3. Deferred session creation is preserved: the optimistic pre-canonical window and the missing-canonical window both still work.
4. Graded against the reference fixture at 100 percent.
5. Colocated `ClaudeAdapter.test.ts` covers the streaming and cancellation paths with a fake transport.

## Notes

Acepe's `cc_sdk_bridge.rs` is 2,941 lines and encodes hard-won detail about tool argument parsing. Read it before writing this, and port the KNOWLEDGE rather than the code.

## Definition of done

- All acceptance criteria above pass.
- Colocated `.test.ts` exists beside every new source file.
- `bun run typecheck` and the full test suite pass.
- `god-architecture-check` was run if this ticket touches canonical session state.
