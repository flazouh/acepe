---
title: "AC-044: Remove neverthrow and zod, repo-wide"
type: rebuild
status: todo
wave: "Wave 7 — Frontend"
size: L
blocked_by: [AC-021]
date: 2026-08-20
---

# AC-044 — Remove neverthrow and zod, repo-wide

**Wave:** Wave 7 — Frontend  ·  **Size:** L  ·  **Blocked by:** AC-021
**Blocks:** nothing

## Goal

The wide refactor. 241 files import neverthrow, 163 import zod. Sequence it expand, migrate, contract; do not force it into vertical slices.

## Files

- `packages/desktop/src/`
- `packages/ui/src/`
- `packages/website/src/`

## Acceptance criteria

1. EXPAND: add Effect and Schema equivalents alongside the existing helpers so nothing breaks. One ticket-sized commit.
2. MIGRATE: convert call sites in batches sized by package, each batch its own commit, with CI green between batches.
3. CONTRACT: delete `neverthrow` and `zod` from every `package.json` once no importer remains.
4. A CI check fails the build if either package reappears in a dependency list.
5. The full test suite passes at every batch boundary, not only at the end.

## Notes

This is the Asana-shaped ticket: mechanical, file-independent, and the existing test suite is a free oracle. Run four agent lanes here and review twice a day.

## Definition of done

- All acceptance criteria above pass.
- Colocated `.test.ts` exists beside every new source file.
- `bun run typecheck` and the full test suite pass.
- `god-architecture-check` was run if this ticket touches canonical session state.
