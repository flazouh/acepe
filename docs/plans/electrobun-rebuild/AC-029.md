---
title: "AC-029: Agent installer from the ACP registry"
type: rebuild
status: todo
wave: "Wave 4 — Providers"
size: M
blocked_by: [AC-023]
date: 2026-08-20
---

# AC-029 — Agent installer from the ACP registry

**Wave:** Wave 4 — Providers  ·  **Size:** M  ·  **Blocked by:** AC-023
**Blocks:** nothing

## Goal

Delete 1,673 lines of hand-maintained installer code in favour of registry data.

## Files

- `packages/server/src/provider/Layers/AgentInstaller.ts`

## Acceptance criteria

1. Agent distribution is read from ACP registry `agent.json` files: per-platform archive URLs, `cmd`, and `args`.
2. Downloads are checksum-verified before extraction.
3. All managed CLIs auto-update to latest, matching the existing standing rule; no manual version pins.
4. `claude --version` parsing tolerates the trailing suffix that breaks naive semver parsing.
5. Agents not in the registry keep a local override entry.

## Notes

The registry covers cursor, github-copilot, codex, claude, opencode, gemini, qwen-code, goose and grok-build among 42 entries.

## Definition of done

- All acceptance criteria above pass.
- Colocated `.test.ts` exists beside every new source file.
- `bun run typecheck` and the full test suite pass.
- `god-architecture-check` was run if this ticket touches canonical session state.
