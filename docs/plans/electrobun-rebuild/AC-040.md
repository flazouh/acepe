---
title: "AC-040: History importers"
type: rebuild
status: todo
wave: "Wave 6 — Services fan-out"
size: L
blocked_by: [AC-018, AC-022]
date: 2026-08-20
---

# AC-040 — History importers

**Wave:** Wave 6 — Services fan-out  ·  **Size:** L  ·  **Blocked by:** AC-018, AC-022
**Blocks:** AC-046

## Goal

Port the three provider JSONL importers. These are the easiest ports and the best first parallel lane.

## Files

- `packages/server/src/history/Layers/ClaudeHistory.ts`
- `packages/server/src/history/Layers/CursorHistory.ts`
- `packages/server/src/history/Layers/OpenCodeHistory.ts`

## Acceptance criteria

1. Each importer decodes provider JSONL with Effect Schema rather than ad-hoc parsing.
2. Importing a real history directory produces byte-identical projections to the Rust importer, graded by the harness.
3. Malformed lines are skipped with a typed warning, never a crash.

## Notes

18,825 Rust lines across the three. Pure parsing with a clean oracle, so run this lane first to calibrate agent throughput.

## Definition of done

- All acceptance criteria above pass.
- Colocated `.test.ts` exists beside every new source file.
- `bun run typecheck` and the full test suite pass.
- `god-architecture-check` was run if this ticket touches canonical session state.
