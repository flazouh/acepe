---
title: "AC-035: Git service"
type: rebuild
status: todo
wave: "Wave 6 — Services fan-out"
size: L
blocked_by: [AC-018, AC-022]
date: 2026-08-20
---

# AC-035 — Git service

**Wave:** Wave 6 — Services fan-out  ·  **Size:** L  ·  **Blocked by:** AC-018, AC-022
**Blocks:** AC-046

## Goal

Port git status, diff, blame, branch and commit operations off `git2` onto a TypeScript implementation.

## Files

- `packages/server/src/git/Layers/GitService.ts`

## Acceptance criteria

1. Every git command the UI calls today is available and graded against its fixture.
2. Large-repository status completes within 2x of the Rust timing on the P0 benchmark repo. If it does not, keep git in the sidecar and say so in the ticket comment.
3. Diff output feeds `@pierre/diffs` unchanged, so the review modal is untouched.

## Notes

6,301 Rust lines today. This is one of the two perf-sensitive ports; measure before you finish it.

## Definition of done

- All acceptance criteria above pass.
- Colocated `.test.ts` exists beside every new source file.
- `bun run typecheck` and the full test suite pass.
- `god-architecture-check` was run if this ticket touches canonical session state.
