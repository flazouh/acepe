---
title: "AC-041: Session store on atoms"
type: rebuild
status: todo
wave: "Wave 7 — Frontend"
size: L
blocked_by: [AC-021, AC-022]
date: 2026-08-20
---

# AC-041 — Session store on atoms

**Wave:** Wave 7 — Frontend  ·  **Size:** L  ·  **Blocked by:** AC-021, AC-022
**Blocks:** AC-042, AC-043, AC-046

## Goal

Rewrite the session store to read projections through atoms instead of holding derived state.

## Files

- `packages/desktop/src/lib/stores/session-store.svelte.ts`
- `packages/desktop/src/lib/stores/session-store-compose.ts`

## Acceptance criteria

1. The store holds NO derived session state. Everything it exposes comes from a snapshot plus applied live events.
2. Live events with a sequence at or below `snapshotSequence` are discarded rather than reapplied.
3. The optimistic send-moment header, title and spark, still renders identically for deferred-creation providers.
4. The PR link toggle keeps working through the new command path.

## Notes

This is the ticket where the GOD architecture gate matters most on the frontend. Run it before starting.

## Definition of done

- All acceptance criteria above pass.
- Colocated `.test.ts` exists beside every new source file.
- `bun run typecheck` and the full test suite pass.
- `god-architecture-check` was run if this ticket touches canonical session state.
