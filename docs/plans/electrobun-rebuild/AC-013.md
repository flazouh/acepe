---
title: "AC-013: projection.sessions"
type: rebuild
status: todo
wave: "Wave 1 — Projections"
size: M
blocked_by: [AC-012]
date: 2026-08-20
---

# AC-013 — projection.sessions

**Wave:** Wave 1 — Projections  ·  **Size:** M  ·  **Blocked by:** AC-012
**Blocks:** AC-015

## Goal

Materialise the session list the sidebar reads: title, project, archived state, provider, timestamps, last activity.

## Files

- `packages/server/src/persistence/Services/ProjectionSessions.ts`
- `packages/server/src/persistence/Layers/ProjectionSessions.ts`
- `packages/server/src/persistence/Migrations/0005_projection_sessions.ts`

## Acceptance criteria

1. Table `projection_sessions` with one row per session, rebuildable from events alone.
2. Session title follows Acepe's existing rules, including artifact stripping and the fallback to the first user message.
3. Archived and deleted sessions are represented explicitly, never by row absence, so the UI can show them.
4. `rebuild('projection.sessions')` reproduces the table byte-identically from the same event log.

## Notes

Acepe's `__session_registry__` placeholder rows do not carry forward. In the event-sourced model a session exists because `SessionCreated` was appended, so the stale-placeholder class of bug disappears by construction.

## Definition of done

- All acceptance criteria above pass.
- Colocated `.test.ts` exists beside every new source file.
- `bun run typecheck` and the full test suite pass.
- `god-architecture-check` was run if this ticket touches canonical session state.
